<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:name=".GainTrackerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="UpTrack"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar.IgnoreDarkMode"
        android:hasFragileUserData="true"
        tools:targetApi="31"
        >

        <!-- Set SplashScreenActivity as the Launcher Activity -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DashboardActivity"
            android:exported="true" />
        <activity android:name=".AddExerciseActivity"
            android:parentActivityName="com.UpTrack.example.UpTrack.DashboardActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
        </activity>
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".viewmodels.ExerciseDetailsActivity"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar" />
    </application>

</manifest>
